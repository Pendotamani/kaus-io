# Lovable AI Gateway key (powers /api/chat -> Gemini).
# Obtain from https://lovable.dev (Cloud > AI) or replace the upstream
# fetch in src/routes/api/chat.ts with a direct Gemini/OpenAI call and
# rename this variable.
LOVABLE_API_KEY=
